apiVersion: apps/v1
kind: Deployment
metadata:
  name: log-output
spec:
  replicas: 1
  selector:
    matchLabels:
      app: logoutput
  template:
    metadata:
      labels:
        app: logoutput
    spec:
      volumes:
        - name: logs
          emptyDir: {}
      containers:
        - name: gen-log-output
          image: stevemcwin/gen-log-output:latest
          volumeMounts:
            - name: logs
              mountPath: /usr/src/app/files
        - name: read-log-output
          image: stevemcwin/read-log-output:latest
          volumeMounts:
            - name: logs
              mountPath: /usr/src/app/files

